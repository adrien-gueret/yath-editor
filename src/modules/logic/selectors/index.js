import { default as rules } from './rules';
import { default as conditionGroups } from './conditionGroups';
import { default as conditions } from './conditions';
import { default as results } from './results';

export default {
    conditionGroups,
    rules,
    conditions,
    results,
};