import { default as name } from './name';
import { default as isConfiguring } from './isConfiguring';
import { default as isTesting } from './isTesting';
import { default as customCSS } from './customCSS';
import { default as customJS } from './customJS';
import { default as otherParameters } from './otherParameters';
import { default as globalSettings } from './globalSettings';

export default {
    customCSS,
    customJS,
    name,
    isConfiguring,
    isTesting,
    otherParameters,
    globalSettings,
};